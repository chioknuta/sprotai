# Everything under out/ is regenerated by the harness in one command, and the
# measurement passes leave hundreds of intermediate PNGs and probe pages. Git
# would carry them forever, and this repo is served by GitHub Pages.
out/*
fit/out/
fit/_probe*.html

# ...except the review page itself, which is the deliverable.
!out/gallery.html
